Upgrade the router then run the following commands as root in the unzipped folder:

dpkg -i deps/* *.deb

cp -r edgeos-dmvpn-conf/* /

systemctl daemon-reload
systemctl enable opennhrp.init

reboot # not necessary but good practice
